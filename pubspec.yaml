name: jaspr_web_cv_portfolio
description: "Jaspr implementation of the Flutter CV/portfolio site"
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  jaspr: ^0.22.3
  jaspr_router: ^0.8.1

dev_dependencies:
  jaspr_builder: ^0.22.2
  build_runner: ^2.4.0
  lints: ^5.0.0

jaspr:
  mode: static
